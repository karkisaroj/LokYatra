[build]
builder = "nixpacks"
buildCommand = "dotnet publish backend.csproj -c Release -o out"

[deploy]
startCommand = "dotnet out/backend.dll"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
